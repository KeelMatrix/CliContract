name: Publish package

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      id-token: write
    env:
      CI: 'true'
      KEELMATRIX_NO_TELEMETRY: '1'
      DOTNET_CLI_TELEMETRY_OPTOUT: '1'
    steps:
      - uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0
      - uses: actions/setup-dotnet@v4.3.1
        with:
          global-json-file: global.json
      - name: Verify tag
        shell: pwsh
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: |
          ./scripts/verify-release-tag.ps1 -Tag $env:TAG_NAME -OutputEnvironmentFile $env:GITHUB_ENV
      - name: Verify release contract
        shell: pwsh
        run: ./scripts/verify-release-contract.ps1 -Version $env:RELEASE_VERSION -Tag $env:GITHUB_REF_NAME
      - name: Verify hermetic acceptance evidence contract
        shell: pwsh
        env:
          GH_TOKEN: ''
        run: ./scripts/test-acceptance-map.ps1
      - name: Build package and run checks
        shell: pwsh
        run: ./scripts/local-gate.ps1 -SkipAcceptanceMapSelfTest
      - name: Verify release artifacts
        shell: pwsh
        run: ./scripts/verify-release-artifacts.ps1 -ArtifactDirectory ./artifacts/packages -Version $env:RELEASE_VERSION
      - name: NuGet login
        uses: NuGet/login@v1
        id: login
        with:
          user: dmitriyzen
      - name: Publish package
        shell: pwsh
        env:
          NUGET_API_KEY: ${{ steps.login.outputs.NUGET_API_KEY }}
        run: |
          $package = "./artifacts/packages/KeelMatrix.CliContract.$($env:RELEASE_VERSION).nupkg"
          $symbols = "./artifacts/packages/KeelMatrix.CliContract.$($env:RELEASE_VERSION).snupkg"
          ./scripts/publish-package.ps1 -Package $package -Symbols $symbols
